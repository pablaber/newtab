VITE_HOSTED=false

# Public browser credentials. Account sync is enabled only when VITE_HOSTED is
# true and both values are present. Never put a service-role key here.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
